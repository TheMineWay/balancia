{
	"name": "scripts",
	"version": "1.0.0",
	"description": "Scripts project",
	"main": "dist/index.js",
	"scripts": {
		"build": "tsc",
		"launch": "pnpm build && node dist/cli.js",
		"generate-packages-indexs": "pnpm build && node dist/packages/generate-indexs/index.js"
	},
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^24.10.4",
		"typescript": "^5.9.3"
	},
	"dependencies": {
		"@inquirer/prompts": "^8.1.0",
		"fs": "0.0.1-security"
	},
	"engines": {
		"node": ">=24",
		"pnpm": ">=10"
	}
}
